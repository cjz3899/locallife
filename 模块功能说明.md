# LocalLife 项目模块功能说明

> 黑马点评-plus 升级版实战项目（Maven 多模块聚合工程）
> 技术栈：Spring Boot 3.5.4 · Java 17 · MyBatis-Plus 3.5.7 · Redis · Redisson 3.52 · Kafka · ShardingSphere 5.3.2 · Sa-Token 1.43

## 一、项目结构总览

```
LocalLife
├── LocalLife-common                    公共基础模块（常量/枚举/异常/工具）
├── LocalLife-core-service              核心业务服务（主启动模块）
├── LocalLife-parameter                 参数模型模块（DTO/VO）
├── LocalLife-sharding                  分库分表组件
├── LocalLife-generator-framework       ID 生成器框架
├── LocalLife-mq-framework              Kafka 消息队列框架
│   ├── LocalLife-mq-common-framework
│   ├── LocalLife-mq-producer-framework
│   └── LocalLife-mq-consumer-framework
├── LocalLife-redis-tool-framework      Redis 工具框架
│   ├── LocalLife-redis-common-framework
│   ├── LocalLife-redis-framework
│   └── LocalLife-redis-rate-limit-framework
├── LocalLife-redisson-framework        Redisson 分布式组件框架
│   ├── LocalLife-redisson-service-framework（聚合父模块）
│   │   ├── LocalLife-redisson-common-framework
│   │   ├── LocalLife-service-lock-framework
│   │   ├── LocalLife-bloom-filter-framework
│   │   └── LocalLife-repeat-execute-limit-framework
│   └── LocalLife-service-delay-queue-framework
├── LocalLife-vue3                      前端项目（Vue3）
└── sql                                 数据库脚本
```

## 二、模块依赖关系

```
LocalLife-core-service（业务入口）
 ├── LocalLife-sharding
 ├── LocalLife-parameter
 ├── LocalLife-mq-producer-framework ──> LocalLife-mq-common-framework ──> LocalLife-common
 ├── LocalLife-mq-consumer-framework ──> LocalLife-mq-common-framework
 ├── LocalLife-redis-rate-limit-framework ──> LocalLife-redis-framework ──> LocalLife-redis-common-framework ──> LocalLife-common
 ├── LocalLife-service-lock-framework ──> LocalLife-redisson-common-framework ──> LocalLife-common
 ├── LocalLife-bloom-filter-framework
 ├── LocalLife-repeat-execute-limit-framework ──> LocalLife-service-lock-framework
 └── LocalLife-id-generator（由 LocalLife-generator-framework 提供）
```

依赖层级核心：**所有框架模块最终都依赖 LocalLife-common**。

## 三、各模块功能详述

### 1. LocalLife-common（公共基础模块）✅ 已实现

项目地基，被所有模块依赖，提供全局复用的基础能力：

| 分类 | 内容 |
|---|---|
| 常量 | `Constant`（业务常量）、`DistributedLockConstants`（分布式锁业务名管理）、`RepeatExecuteLimitConstants`（防重复幂等业务名管理） |
| 枚举 | `BaseCode`（接口返回码）、`OrderStatus`（订单状态）、`VoucherStatus`（优惠券状态）、`StockUpdateType`（库存操作类型）、`BusinessType`、`LogType`、`ReconciliationStatus`（对账状态）、`SubscribeStatus`、`SeckillVoucherOrderOperate` |
| 异常 | `BaseException`（基础异常）、`ArgumentException`（参数异常）、`ArgumentError`（参数错误）、`HmdpFrameException`（业务异常） |
| 工具 | `DateUtils`（日期工具）、`SpringUtil`（Spring 容器工具）、`BaseParameterHolder`（线程绑定参数） |
| Spring 钩子 | 通过 `spring.factories` 注册 `SpringEnvironment`（EnvironmentPostProcessor）、`SpringUtil`（ApplicationContextInitializer） |

### 2. LocalLife-core-service（核心业务服务）✅ 已实现

项目主启动模块（`LocalLifeApplication`），承载全部业务逻辑：14 个实体、15 个 Mapper、17 个 Service 接口及实现。

**业务能力：**

| 领域 | 说明 |
|---|---|
| 用户体系 | 手机号+验证码登录、JWT/Token 双拦截器（`LoginInterceptor` + `RefreshTokenInterceptor`）、密码加密（`PasswordEncoder`） |
| 商铺缓存 | `CacheClient` 封装缓存穿透/击穿/雪崩防护、缓存预热、`SimpleRedisLock` 手动锁 |
| 优惠券秒杀 | Lua 脚本原子扣库存（`seckillVoucher.lua`、`seckill.lua`、`seckillVoucherRollBack.lua`、`unlock.lua`）、Redisson 分布式锁、异步下单（Kafka/延迟队列）、订单状态机 |
| 订单路由 | `VoucherOrderRouter` 按路由规则落库，配合 ShardingSphere 分库分表（`shardingsphere.yaml` 配置 2 库 `hmdp_0`/`hmdp_1`） |
| 社交 | 博客发布/点赞/热榜、关注（feed 流）、博客评论 |
| 对账与告警 | 定时对账（`ReconciliationTaskService`）、对账日志（`VoucherReconcileLog`）、失败回滚日志与短信/邮件告警（`RollbackAlertService`） |

**核心配置：** `LocalLifeCommonAutoConfig`（自动配置）、`MvcConfig`（拦截器注册）、`JacksonCustom`/`JsonCustomSerializer`/`DateJsonDeserializer`（JSON 序列化定制）、`WebExceptionAdvice`（全局异常处理）

### 3. LocalLife-parameter（参数模型模块）✅ 已实现

独立的请求/响应参数封装，与业务服务解耦：15 个 DTO + 1 个 VO，覆盖优惠券（`VoucherDto`、`SeckillVoucherDto`）、订单（`VoucherOrderDto`、`GetVoucherOrderDto`、`CancelVoucherOrderDto`）、订单路由（`GetVoucherOrderRouterDto`）、订阅（`VoucherSubscribeDto`、`VoucherSubscribeBatchDto`、`DelayVoucherReminderDto`）、对账（`VoucherReconcileLogDto`）等场景。

### 4. LocalLife-sharding（分库分表组件）⚠️ 骨架（仅剩死代码）

基于 ShardingSphere JDBC 5.3.2 的分库分表基础设施，依赖 `LocalLife-common`、`spring-boot-starter-web/validation`。实际分片配置位于 core-service 的 `shardingsphere.yaml`（按订单路由字段分 2 库）。

> 注意：当前模块 `src` 下仅有一个 `org/yaml/snakeyaml/representer/Representer.java`（SnakeYAML 官方 Representer 的拷贝，无任何引用），属于遗留死代码，分库分表能力实际由 core-service 的配置驱动。

### 5. LocalLife-generator-framework（ID 生成器框架）🔧 骨架

基于 Redis 的分布式 ID 生成框架（模块名原名 `hmdp-id-generator`），依赖 `spring-boot-starter-data-redis`、`LocalLife-common`、commons 工具库。预期提供全局唯一 ID 生成能力（如雪花算法、Redis 自增序列），当前仅有空 `AutoConfiguration.imports` 占位，源码待实现。

### 6. LocalLife-mq-framework（Kafka 消息队列框架）🔧 骨架

| 子模块 | 职责 |
|---|---|
| LocalLife-mq-common-framework | 消息模型、公共配置（依赖 spring-kafka、LocalLife-common） |
| LocalLife-mq-producer-framework | 消息生产者封装 |
| LocalLife-mq-consumer-framework | 消息消费者封装（注册 AutoConfiguration 扩展点） |

用于秒杀订单的异步下单/延迟提醒等场景。当前仅有空 imports 占位，源码待实现。

### 7. LocalLife-redis-tool-framework（Redis 工具框架）🔧 骨架

| 子模块 | 职责 |
|---|---|
| LocalLife-redis-common-framework | Redis 基础封装（spring-boot-starter-data-redis） |
| LocalLife-redis-framework | Redis 核心工具（缓存操作等），依赖 redis-common |
| LocalLife-redis-rate-limit-framework | 接口限流组件，集成 Actuator + Prometheus 监控指标，含测试类 `RedisRateLimitHandlerTest` |

### 8. LocalLife-redisson-framework（Redisson 分布式组件框架）🔧 骨架

`redisson 封装 / 分布式服务工具`，聚焦高并发下的分布式组件：

| 子模块 | 职责 |
|---|---|
| LocalLife-redisson-common-framework | 基础配置：redisson-spring-boot-starter、spring-data-redis、AOP、Caffeine 本地缓存 |
| LocalLife-service-lock-framework | 分布式锁（注解式锁封装） |
| LocalLife-bloom-filter-framework | 布隆过滤器（缓存穿透防护） |
| LocalLife-repeat-execute-limit-framework | 防重复执行/幂等控制（依赖 service-lock） |
| LocalLife-service-delay-queue-framework | 延迟队列（优惠券过期提醒等） |

## 四、当前问题提示（仅记录，未处理）

1. **parameter 模块 spring.factories 失效**：`LocalLife-parameter/src/main/resources/META-INF/spring.factories` 引用了 common 的 `SpringEnvironment`/`SpringUtil`，但其 pom **未声明依赖 LocalLife-common**，该注册无法生效。
2. **sharding 模块死代码**：`Representer.java` 无任何引用，且基于 SnakeYAML 1.x API 编写，与项目实际生效的 snakeyaml 2.4 不兼容。
3. **框架模块均为骨架**：generator / mq / redis-tool / redisson 各子模块只有 pom 与空 `AutoConfiguration.imports` 占位，核心能力（限流、锁、布隆过滤器、延迟队列、消息封装、ID 生成）的自动配置类待实现。
4. **本地仓库未安装模块 jar**：`~/.m2/repository/com/junzhecai/` 下无各模块构件，需先 `mvn install` 才能正常编译依赖。
